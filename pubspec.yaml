name: friflex
description: Weather


publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^0.13.5
  test: ^1.21.4
  flutter_bloc: ^8.1.2
  #Подключаем репозиторий к проекту
  search_forecast_repository:
    path: packages/search_forecast_repository

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true